"""通訊軟體 bot：把派工台的「查」帶到手機聊天室，但不帶走派工台的界線以外的東西。

分四支：

    linking.py   誰可以看——聊天室綁到派工台帳號，一次性碼、可撤銷、跟著帳號停用
    content.py   看到什麼——待稽核清單與答詢的文字，與派工台同一套排序與用詞
    mapimage.py  地圖——伺服器端直接畫成 PNG，不需要 HTTPS、不需要打開瀏覽器
    telegram.py  怎麼送——long polling，本機就能跑，不需要公開網址

LINE 之後接進來時只換傳輸層（webhook），前三支共用。

三條界線與 MaiCoin 那版的差別，理由在各自的模組說明：

1. **沒綁定就沒有資料。** MaiCoin 的 bot 誰傳訊息都回；這裡的名單是真實機構。
2. **只在私訊。** 群組裡的其他人沒有登入過派工台。
3. **只查、不做。** 答詢走 `/api/chat` 的 `answer()`（檢索型），不接會操作畫面或
   寫入資料的 agent；草稿、建議書一律留在派工台。
"""
