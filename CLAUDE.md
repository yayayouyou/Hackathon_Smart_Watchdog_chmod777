# CLAUDE.md

競賽專案：小小守護員 Smart Watchdog（2026 新北市 AI 黑客松，教育局組）。
教保機構風險預警系統，AI × 鑑識會計。決賽 2026/9/12–13。

## 環境

```bash
.venv/bin/python          # 專案 venv（系統 python 是 3.9，無 PDF 套件）
PYTHONPATH=src            # 匯入 smart_watchdog 需要
```



## 抽取的鐵則

`extract/schema.py` 的規則不是風格偏好，違反會產生對真實機構的錯誤財務陳述：

- **空白格填 `null`，絕不填 `0`。** 「業務發展費預算欄空白」＝未編列預算（稽查發現），
  填 0 ＝謊稱編列了零元。破折號（—、-、`$ -`）也是空白。
  ⚠️ 這個錯誤我們自己犯過：ground truth 曾把 `應付票據` 的 `$ -` 記成 0，
  由抽取模型交叉比對流動負債合計後指出（見 ground truth 的 `corrections`）。
- **括號是負數**：`(1,988,771)` → `-1988771`。
- **欄位靠視覺位置對齊，不是靠順序。** 跳過前欄的列必須在前面補 `null`。
- **看不清就填 null 並記在 `issues`，絕不猜數字。** 缺漏可由人工補；
  看似合理的錯誤數字無法被發現。

## 年度對應（容易搞錯）

- **學年度 N 的資產負債表基準日是 (N+1)/7/31。**
  例：`N01安溪_113學年度財務報告.pdf` 的資產負債表主要欄位是 **114/7/31**，
  比較欄才是 113/7/31。收支餘絀表期間則為 113.8.1–114.7.31。
- 公校決算書用**年度**（112/113/114），非學年度，兩者不可直接對齊。
- 收費明細 `slip113` 指學年度 113，申報時點約在西元 2024 年。

## 資料集的坑（已驗證，不要重新踩）

1. **zip 檔名是 Big5**，`unzip` 會失敗。用
   `i.orig_filename.encode('cp437').decode('big5')`。
2. **132 份非營利財報是純掃描影像**，`get_text()` 回傳空字串。必須走視覺模型。
3. **公校決算書 218 頁（1.9%）文字層是垃圾** — `CIDFont+F1` / `Identity-H`
   缺 ToUnicode，抽出 U+E000–U+F8FF 私有造字區字碼，且每頁對應表不同、無法解碼。
   偵測方式見 `scripts/survey_text_quality.py::pua_ratio`。
   **`pdf_survey.csv` 的 `text_ratio` 不能用來判斷可抽取性**（它把 PUA 亂碼算成有效文字）。
4. **`get_text()` 的欄位順序是亂的** — 財務表格會交錯。必須用
   `pdf_utils.page_rows()` 依 word bounding box 重建列。
5. **公校分冊切割靠頁尾** `13601-4` = `<分基金代號>-<頁碼>`，不要靠目錄。
   `136xx` 才是市立幼兒園。

## 外部資料

`scrape/registry.py` 以 `kiang.github.io/ap.ece.moe.edu.tw` 鏡像為主來源，
**不是**因為方便，而是因為官方裁罰頁有保存期限會下架，只爬官方會拿到截斷標籤。
改回官方端點前先讀 `docs/research/03-external-data.md` §1.2。

兩個粒度陷阱：
- `punish_all.json` 上層 key 是**人**（`負責人：X`／`行為人：X`），不是機構。
  要用 record 內的 `id` 反轉成機構索引。
- 公立園主檔是**分班層級**（67 筆），決算書是**分基金層級**（21 筆）。
  join 前用 `registry.parent_of()` 聚合。

## 建模紀律

- **時序切分，不可隨機切分。** 歷史裁罰是最強特徵，隨機切分會讓同一園的未來裁罰
  洩漏進訓練集，得到虛高 AUC。`features/build.py` 的每個函式都吃 `as_of` 並拒絕
  看它之後的資料——加新特徵時要維持這個性質（車輛特徵曾漏過，產生負車齡才被發現）。
- **⚠️ 任何在公立／非營利／私立間有結構性差異的特徵，分層前都會看起來很強。**
  這個陷阱已出現兩次：`monthly` 從 p=0.00066 掉到私立園內 0.050；
  `max_jump`（收費漲幅）從 rbc=+0.399 掉到 +0.097。
  **一律先分層再下結論**，且模型中要與 `type` 同時放入。
- **一致性失敗代表檢查寫錯，不是資料錯。** 恆等式在 58/58 全部失敗時，
  是我們對欄位語意的假設錯了（「比較增減」是決算−預算，不是年度比較）。
- **公校與非營利分群算 z-score。** 財源結構差太多（公校約 89% 靠公庫撥款，
  標準差僅 0.031）。
- **同儕比較要控制年度與設立年資。** 學雜費執行率有全市系統性下滑
  （112→114 中位數 −5.5pp, Wilcoxon p=0.026），用絕對門檻會在 114 年大量誤標；
  新設園代管財產占比天生偏高（崁頂 54% vs 安溪 10%）。
- **不用 12 個正樣本訓監督式模型**（59 公共化園）。改當訊號驗證集。
- 非營利園是**成本分攤制**，餘絀接近零、負債占比高是制度設計而非經營不善。
  不要套用上市公司比率門檻。

## 輸出定位

系統輸出是「建議查核」的**稽查優先序**，不是「疑似不法」的違法認定。
所有面向使用者的文字都要遵守這個用詞界線；不確定時標「資料不足」而非「低風險」。
理由見 `docs/architecture/aws-architecture.md` §6。
